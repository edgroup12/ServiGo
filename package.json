{
  "name": "servigo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:realtime": "npm run start --prefix realtime",
    "test": "npm run test --prefix backend && npm run test --prefix realtime && npm run test --prefix frontend",
    "lint": "npm run check --prefix backend && npm run check --prefix realtime && npm run lint --prefix frontend",
    "build": "npm run build --prefix frontend",
    "quality": "npm run test && npm run lint && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}