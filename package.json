{
  "name": "servigo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "lint": "npm run check --prefix backend && npm run lint --prefix frontend",
    "build": "npm run build --prefix frontend",
    "quality": "npm run test && npm run lint && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}